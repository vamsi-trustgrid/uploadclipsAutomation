{
  "name": "instagram-downloader",
  "version": "1.0.0",
  "description": "Monorepo / tool manager for Instagram scraper and video uploader",
  "scripts": {
    "install:scraper": "cd instagramScraper && npm install",
    "install:uploader": "cd uploadSystem && npm install",
    "install:all": "npm run install:scraper && npm run install:uploader",
    "start:scraper": "cd instagramScraper && npm start",
    "start:uploader": "cd uploadSystem && npm start",
    "start": "npm run start:scraper && npm run start:uploader"
  }
}
